{
  "_comment": "SAM log highlight rules. Edit this file to add/adjust highlighting. Each rule: name, level (error|warn|info), pattern (JavaScript regex source, case-insensitive). Whole-line tint follows the highest level matched on that line.",
  "rules": [
    {
      "name": "error",
      "level": "error",
      "pattern": "\\b(?:error|err|fail(?:ed|ure)?|fatal|panic|exception|nak|crc(?:\\s*error)?|fault|invalid|corrupt(?:ed)?|denied)\\b"
    },
    {
      "name": "warning",
      "level": "warn",
      "pattern": "\\b(?:warn(?:ing)?|timeout|timed?\\s*out|retry|retries|retrying|busy|skip(?:ped)?)\\b"
    }
  ]
}
